Implementations is done in Django 1.10 version so it requires python2.7 version.
Execute pip install -r requirement.txt 
Sqlite database is used. It already has sqlite file which contains some data.
Run the server using "python manage.py 0:8000"